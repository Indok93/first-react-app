import { Navbar } from '../../components/Navbar/Navbar';

export const Header = () => {
    return (
        <header>
            <Navbar></Navbar>
            <h1>First React App</h1>
        </header>
    )
}